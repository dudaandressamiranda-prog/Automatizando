# Copie para .env e preencha (Settings → API no painel do Supabase).
# Aqui vai a chave `anon` (pública) — NUNCA a service_role.

VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=
