// Copie este arquivo para "config.h" (na mesma pasta) e preencha com os seus dados.
// O arquivo config.h NÃO deve ser commitado (já está no .gitignore).

#define WIFI_SSID     "SUA_REDE_WIFI"
#define WIFI_PASSWORD "SUA_SENHA_WIFI"

// Dados do broker MQTT (ex.: HiveMQ Cloud, free tier)
#define MQTT_HOST     "xxxxxxxx.s1.eu.hivemq.cloud"
#define MQTT_PORT     8883
#define MQTT_USER     "usuario_mqtt"
#define MQTT_PASSWORD "senha_mqtt"

// Identificador único do seu portão (use algo difícil de adivinhar)
#define DEVICE_ID     "portao-casa-XXXXX"
